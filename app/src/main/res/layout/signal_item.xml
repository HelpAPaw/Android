<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/signal_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="8dp"
    android:paddingHorizontal="@dimen/activity_horizontal_margin"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/signal_item_img"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/ic_paw"
        android:contentDescription="@string/txt_signal_photo_description" />

    <TextView
        android:id="@+id/signal_item_title"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginStart="10dp"
        android:gravity="center_vertical"
        android:layout_toEndOf="@id/signal_item_img"
        android:layout_toStartOf="@id/signal_item_date"
        android:layout_centerVertical="true"
        android:textSize="16sp"
        android:ellipsize="end"
        android:lines="2"
        tools:text="Title that is long enough to be spread across several lines and show what happens when there is not enough space." />

    <TextView
        android:id="@+id/signal_item_date"
        android:layout_width='80dp'
        android:layout_height="50dp"
        android:layout_marginStart="10dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:ellipsize="end"
        android:lines="2"
        tools:text="02.04.2022, 07:00 PM" />
</RelativeLayout>

